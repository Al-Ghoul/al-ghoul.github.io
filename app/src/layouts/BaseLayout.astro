---
import BaseHead from "./BaseHead.astro";

interface Props {
  locale: string;
  title: string;
}

const { locale, title } = Astro.props;
---
<html lang={locale}>
  <BaseHead locale={locale} title={title}>
    <slot />
  </BaseHead>
  <slot name="body" />
</html>
